# Copy to .env.local.

# Base URL of 2026-agendic-back, no trailing slash. Server-only: never prefix it with NEXT_PUBLIC_.
# The back listens on 3000 by default; start it first and `next dev` moves itself to 3001.
API_BASE_URL=http://localhost:3000
